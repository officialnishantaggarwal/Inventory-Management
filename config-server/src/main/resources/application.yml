spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/officialnishantaggarwal/ecommerce-config-server
          username: #officialnishantaggarwal
          password: #github password generated through token
          default-label: master

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka



